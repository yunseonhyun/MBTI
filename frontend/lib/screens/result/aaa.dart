import 'package:flutter/material.dart';

class aaa extends StatelessWidget {
  const aaa({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('aaa is working'),
      ),
    );
  }
}
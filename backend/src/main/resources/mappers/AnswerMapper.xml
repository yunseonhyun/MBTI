<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.mbti.answer.mapper.AnswerMapper">

    <select id="selectAll" resultType="Answer">
        SELECT * FROM mbti_answers ORDER BY id
    </select>

    <select id="selectById" resultType="Answer">
        SELECT * FROM mbti_answers WHERE id = #{id}
    </select>

    <select id="selectByResultId" resultType="Answer">
        SELECT * FROM mbti_answers WHERE result_id = #{resultId} ORDER BY id
    </select>

    <insert id="insert" parameterType="Answer" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO mbti_answers (result_id, question_id, selected_option, selected_type)
        VALUES (#{resultId}, #{questionId}, #{selectedOption}, #{selectedType})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO mbti_answers (result_id, question_id, selected_option, selected_type)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.resultId}, #{item.questionId}, #{item.selectedOption}, #{item.selectedType})
        </foreach>
    </insert>

    <update id="update" parameterType="Answer">
        UPDATE mbti_answers
        SET result_id = #{resultId},
            question_id = #{questionId},
            selected_option = #{selectedOption},
            selected_type = #{selectedType}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM mbti_answers WHERE id = #{id}
    </delete>

    <delete id="deleteByResultId">
        DELETE FROM mbti_answers WHERE result_id = #{resultId}
    </delete>

</mapper>
